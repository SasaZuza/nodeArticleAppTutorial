.messages
    each type in Object.keys(messages)
        each message in messages[type]
            div(class="alert alert-"+type) #{message}
// Setting up messages as alert box and looping through them with displaying as errors and success